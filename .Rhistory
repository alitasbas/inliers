round(1.1)
round(1.5)
round(1.523123)
round(1.523123, 1)
round(1.523123, 0)
inlier_gen_akkaya <- function(sample, delta = 0.8, inlier_ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in range(n)) {
index <- -1 * index
sample[index] <- xbar + (-delta)^i * sigma
}
}
library(dplyr)
set.seed(238)
inlier_gen <- function(alpha, beta, n=1000, min, max) {
all_sample <- rnorm(n*100, alpha, beta)
index <- ifelse(between(all_sample, alpha - beta, alpha + beta) & between(all_sample, min, max), TRUE, FALSE)
real_sample <- sample(all_sample[index], n) # replace = FALSE or TRUE?
return(real_sample)
}
inlier_gen_akkaya <- function(sample, delta = 0.8, inlier_ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in range(n)) {
index <- -1 * index
sample[index] <- xbar + (-delta)^i * sigma
}
}
compare_dists <- function(main_sample, inliers, ratio=0.1) {
n <- length(main_sample)
u <- as.integer(runif(n) > ratio)
print(sum(u))
contaminated_sample <- u * main_sample + (1 - u) * inliers # affter this step we lose trace of inlier points
main_dens <- density(main_sample)
inlier_dens <- density(inliers)
max_y <- max(main_dens$y, inlier_dens$y)
par(mfrow=c(1, 2))
plot(main_dens, col = "blue", lwd = 2,
ylim = c(0, max_y))
lines(inlier_dens, col = "red", lwd = 2)
# legend("topright", legend = c("normal", "inlier"), col = c("blue", "red"), lwd = 2)
plot(density(c(contaminated_sample)), col="red", lwd=2)
lines(density(main_sample), col = "blue", lwd = 2)
# legend("topright", legend = c("clean", "contaminated"), col = c("blue", "red"), lwd = 2)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, inlier_ratio = 0.1)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in range(n)) {
index <- -1 * index
sample[index] <- xbar + (-delta)^i * sigma
}
}
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
hist(inliers_akkaya)
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in range(n)) {
index <- -1 * index
sample[index] <- xbar + (-delta)^i * sigma
}
return(sample)
}
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers_akkaya)
length(inliers_akkaya)
inliers_akkaya
inliers[1]
inliers[-1]
3 %% 2
1 %% 2
0 %% 2
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in range(n)) {
if(i %% 2 == 0) {
index <- i
} else {
index <- n - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
source("D:/ODTU/Sem 7/STAT 499/R/inlier_generation.R", echo=TRUE)
inliers_akkaya
norm_sample
sort(norm_sample)
for (i in range(n)) {}
for (i in range(n)) {
print(i)
}
for (i in length(n)) {
print(i)
}
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in 1:length(n)) {
if(i %% 2 == 0) {
index <- i
} else {
index <- n - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
inliers_akkaya
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
hist(inliers_akkaya)
hist(norm_sample)
plot(density(norm_sample))
plot(density(inliers_akkaya))
inliers_akkaya
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in 1:length(n)) {
if(i %% 2 == 1) {
index <- abs(i - 2)
} else {
index <- n - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
compare_dists <- function(main_sample, inliers, ratio=0.1) {
n <- length(main_sample)
u <- as.integer(runif(n) > ratio)
print(sum(u))
contaminated_sample <- u * main_sample + (1 - u) * inliers # after this step we lose trace of inlier points
main_dens <- density(main_sample)
inlier_dens <- density(inliers)
max_y <- max(main_dens$y, inlier_dens$y)
par(mfrow=c(1, 2))
plot(main_dens, col = "blue", lwd = 2,
ylim = c(0, max_y))
lines(inlier_dens, col = "red", lwd = 2)
# legend("topright", legend = c("normal", "inlier"), col = c("blue", "red"), lwd = 2)
plot(density(c(contaminated_sample)), col="red", lwd=2)
lines(density(main_sample), col = "blue", lwd = 2)
# legend("topright", legend = c("clean", "contaminated"), col = c("blue", "red"), lwd = 2)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
plot(density(norm_sample))
plot(density(inliers_akkaya))
inliers_akkaya == norm_sample
inliers_akkaya
norm_sample
sort(norm_sample)
inliers_akkaya
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in 1:length(n)) {
if(i %% 2 == 1) {
index <- i %% 2
} else {
index <- length(sample) - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
compare_dists <- function(main_sample, inliers, ratio=0.1) {
n <- length(main_sample)
u <- as.integer(runif(n) > ratio)
print(sum(u))
contaminated_sample <- u * main_sample + (1 - u) * inliers # after this step we lose trace of inlier points
main_dens <- density(main_sample)
inlier_dens <- density(inliers)
max_y <- max(main_dens$y, inlier_dens$y)
par(mfrow=c(1, 2))
plot(main_dens, col = "blue", lwd = 2,
ylim = c(0, max_y))
lines(inlier_dens, col = "red", lwd = 2)
# legend("topright", legend = c("normal", "inlier"), col = c("blue", "red"), lwd = 2)
plot(density(c(contaminated_sample)), col="red", lwd=2)
lines(density(main_sample), col = "blue", lwd = 2)
# legend("topright", legend = c("clean", "contaminated"), col = c("blue", "red"), lwd = 2)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
inliers_akkaya
sort(norm_sample)
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in n) {
if(i %% 2 == 1) {
index <- i %% 2
} else {
index <- length(sample) - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
compare_dists <- function(main_sample, inliers, ratio=0.1) {
n <- length(main_sample)
u <- as.integer(runif(n) > ratio)
print(sum(u))
contaminated_sample <- u * main_sample + (1 - u) * inliers # after this step we lose trace of inlier points
main_dens <- density(main_sample)
inlier_dens <- density(inliers)
max_y <- max(main_dens$y, inlier_dens$y)
par(mfrow=c(1, 2))
plot(main_dens, col = "blue", lwd = 2,
ylim = c(0, max_y))
lines(inlier_dens, col = "red", lwd = 2)
# legend("topright", legend = c("normal", "inlier"), col = c("blue", "red"), lwd = 2)
plot(density(c(contaminated_sample)), col="red", lwd=2)
lines(density(main_sample), col = "blue", lwd = 2)
# legend("topright", legend = c("clean", "contaminated"), col = c("blue", "red"), lwd = 2)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
sort(norm_sample)
inliers_akkaya
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in 1:n) {
if(i %% 2 == 1) {
index <- i %% 2
} else {
index <- length(sample) - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
compare_dists <- function(main_sample, inliers, ratio=0.1) {
n <- length(main_sample)
u <- as.integer(runif(n) > ratio)
print(sum(u))
contaminated_sample <- u * main_sample + (1 - u) * inliers # after this step we lose trace of inlier points
main_dens <- density(main_sample)
inlier_dens <- density(inliers)
max_y <- max(main_dens$y, inlier_dens$y)
par(mfrow=c(1, 2))
plot(main_dens, col = "blue", lwd = 2,
ylim = c(0, max_y))
lines(inlier_dens, col = "red", lwd = 2)
# legend("topright", legend = c("normal", "inlier"), col = c("blue", "red"), lwd = 2)
plot(density(c(contaminated_sample)), col="red", lwd=2)
lines(density(main_sample), col = "blue", lwd = 2)
# legend("topright", legend = c("clean", "contaminated"), col = c("blue", "red"), lwd = 2)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
sort(norm_sample)
inliers_akkaya
library(dplyr)
set.seed(238)
inlier_gen <- function(alpha, beta, n=1000, min, max) {
all_sample <- rnorm(n*100, alpha, beta)
index <- ifelse(between(all_sample, alpha - beta, alpha + beta) & between(all_sample, min, max), TRUE, FALSE)
real_sample <- sample(all_sample[index], n) # replace = FALSE or TRUE?
return(real_sample)
}
s <- rnorm(100)
n <- round(length(s) * 0.1)
n
s <- sort(s)
index = 1
for (i in 1:n) {}
for (i in 1:n) {
print(i)
}
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i %% 2
print(index)
}
}
index <- i // 2
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i // 2
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i // 2
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i %/% 2
print(index)
}
}
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i %/% 2 + 1
print(index)
}
}
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i %/% 2 + 1
print(index)
} else {
index <- length(sample) - (i/2) + 1
print(index)
}
}
for (i in 1:n) {
if (i %% 2 == 1) {
index <- i %/% 2 + 1
print(index)
} else {
index <- length(s) - (i/2) + 1
print(index)
}
}
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in 1:n) {
if(i %% 2 == 1) {
index <- i %/% 2 + 1
} else {
index <- length(sample) - (i/2) + 1
}
sample[index] <- xbar + (-delta)^index * sigma
}
return(sample)
}
compare_dists <- function(main_sample, inliers, ratio=0.1) {
n <- length(main_sample)
u <- as.integer(runif(n) > ratio)
print(sum(u))
contaminated_sample <- u * main_sample + (1 - u) * inliers # after this step we lose trace of inlier points
main_dens <- density(main_sample)
inlier_dens <- density(inliers)
max_y <- max(main_dens$y, inlier_dens$y)
par(mfrow=c(1, 2))
plot(main_dens, col = "blue", lwd = 2,
ylim = c(0, max_y))
lines(inlier_dens, col = "red", lwd = 2)
# legend("topright", legend = c("normal", "inlier"), col = c("blue", "red"), lwd = 2)
plot(density(c(contaminated_sample)), col="red", lwd=2)
lines(density(main_sample), col = "blue", lwd = 2)
# legend("topright", legend = c("clean", "contaminated"), col = c("blue", "red"), lwd = 2)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
hist(inliers)
hist(norm_sample)
compare_dists(norm_sample, inliers, ratio=1/10)
sort(norm_sample)
inliers_akkaya
inlier_gen_akkaya <- function(sample, delta = 0.8, ratio) {
n <- round(length(sample) * ratio, 0)
xbar <- mean(sample)
sigma <- sd(sample)
sample <- sort(sample)
index <- 1
for (i in 1:n) {
if(i %% 2 == 1) {
index <- i %/% 2 + 1
} else {
index <- length(sample) - (i/2) + 1
}
sample[index] <- xbar + (-delta)^i * sigma # ^index or i
}
return(sample)
}
norm_sample <- rnorm(1000, 21, 2.5)
inliers <- inlier_gen(21, 1.25, 1000, min=20, max=22)
inliers_akkaya <- inlier_gen_akkaya(norm_sample, ratio = 0.1)
sort(norm_sample)
inliers_akkaya
